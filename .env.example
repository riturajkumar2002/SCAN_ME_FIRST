VIRUSTOTAL_API_KEY=your_virustotal_api_key_here

# Copy this file to .env and replace the value with your actual VirusTotal API key.
# For Vercel deployments, set the `VIRUSTOTAL_API_KEY` environment variable in the project settings instead of using a .env file.